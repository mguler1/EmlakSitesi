@model IEnumerable<EmlakSitesi.Models.Ilan>
@{ int i = 0; }
@foreach (var item in Model)
        {
            i++;
            var c = 1;
            var active = i == 1 ? "active" : "";
<div class="item @active">
    @foreach (var rsm in ViewBag.imgs)
    {
        if (rsm.IlanId == item.IlanID)
        {
            if (c == 1)//sadece ilk resim gelsin
            {
    <a href="/Home/Detay/@item.IlanID">  <img src="~/Content/Resimler/@rsm.ResimAd" alt="..." style="width:1150px; height:400px"></a>
                c++;
            }

        }
    }
</div>
}
